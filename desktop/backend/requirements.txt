# Runtime deps for the desktop backend. Same engine as the plugin, so this
# mirrors the repo-root requirements.txt. The vendored decky_plugin/py_modules
# copies are Deck-only binary builds (cpython-311) and are NOT used here.
requests>=2.28.0
watchdog>=3.0.0
psutil>=5.9.0
cryptography>=3.4.8
Pillow>=10.0.0
# QR encoding for device pairing. Pure Python — unlike Pillow it has no C
# extension, but it still belongs here: py_modules is not on the desktop path.
qrcode>=7.4
